# OpenWrt `shadowsocks-libev` Payload

These files are copied to the secondary OpenWrt `shadowsocks-libev` consumer profile.

Current file:

- `ss-domain-filter.init`
  - init service for the domain-to-IPv4 snapshot helper used by that consumer
