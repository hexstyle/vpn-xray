# Server Files

This folder contains the server-side template used for the VPS.

Current file:

- `xray-vps-config.template.json`

It is rendered by:

- [`scripts/deploy-vps-config.sh`](../scripts/deploy-vps-config.sh)

using values from:

- `config/vps.env`
