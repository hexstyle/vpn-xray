# Script Helpers

Small shared shell helpers used by the top-level scripts live here.

Current helper:

- `required-env.sh`
  - load env files
  - enforce required variables
  - reject placeholder values
  - derive a GitHub repo slug from configured repo URLs
